app {
  name = "config"
  db {
    url = "jdbc:h2:mem:test;INIT=RUNSCRIPT FROM 'classpath:ddl.sql"
    driver = "org.h2.Driver"
  }
  http {
    host = "localhost"
    port = 9999
  }
}

slick = {
  url = "jdbc:h2:~/.scala/db/slick"
  driver = org.h2.Driver
  connectionPool = disabled
  keepAliveConnection = true
}

sorm = {
  url = "jdbc:h2:~/.scala/db/sorm"
}